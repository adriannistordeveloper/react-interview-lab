const Navbar = () => {
  return (
    <nav
      style={{
        height: "60px",
        display: "flex",
        alignItems: "center",
        padding: "0 20px",
        borderBottom: "1px solid #ddd",
        fontWeight: 600
      }}
    >
      React Interview Lab
    </nav>
  );
};

export default Navbar;